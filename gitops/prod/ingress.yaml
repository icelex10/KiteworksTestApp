apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kiteworks-app
  namespace: kiteworks-prod
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - kiteworkstest-prod.eastus2.cloudapp.azure.com
      secretName: kiteworks-prod-tls
  rules:
    - host: kiteworkstest-prod.eastus2.cloudapp.azure.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: kiteworks-app
                port:
                  number: 80