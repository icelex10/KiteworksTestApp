apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kiteworks-app
  namespace: kiteworks-dev
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - kiteworkstest-dev.centralus.cloudapp.azure.com
      secretName: kiteworks-dev-tls
  rules:
    - host: kiteworkstest-dev.centralus.cloudapp.azure.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: kiteworks-app
                port:
                  number: 80