apiVersion: v1
kind: Service
metadata:
  name: kiteworks-app
  namespace: kiteworks-dev
  labels:
    app: kiteworks-app
spec:
  type: ClusterIP
  selector:
    app: kiteworks-app
  ports:
    - name: http
      port: 80
      targetPort: 9898
      protocol: TCP
